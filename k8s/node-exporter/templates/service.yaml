apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-exporter.fullname" . }}
  labels:
    app: node-exporter
spec:
  selector:
    app: node-exporter
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
  clusterIP: None
